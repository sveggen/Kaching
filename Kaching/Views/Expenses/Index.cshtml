<partial name="/Views/Shared/_GroupNavbar.cshtml" />

@model List<Kaching.ViewModels.ExpenseVm>

<h1>List of Expenses</h1>  
<a asp-action="Create">Create</a>  

<table class="ui unstackable celled table">
    <thead>
    <tr><th>Date</th>
        <th>Item</th>
        <th>Paid</th>
        <th></th>
    </tr></thead>
    <tbody>
    @foreach (var expense in Model)
    {
        <tr>
            <td>@expense.PaymentDate</td>
            <td>@expense.CurrencySymbol @expense.Price of @expense.CategoryName
            <td>@expense.Paid</td>
            <td>
                <a asp-action="Details">Details</a>
            </td>
        </tr>
    }
    </tbody>
</table>