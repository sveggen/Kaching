@model Kaching.ViewModels.SettlementVm


Total Expenses: @Model.Expenses.Sum(X => X.Price)



@{
    var settlementPeriods = Model.Expenses
        .Select(x => x.DueDate.ToString()).Distinct();
    foreach (var x in settlementPeriods)
    {
        @x;
    }
}